"use strict";

const EmailsEditor = ({container, ...options}) => {
    console.log(`Initialization -- Render EmailsEditor in ${container.id} with options ${JSON.stringify(options)}`);



    console.log(`Initialization -- Finishing rendering`);
};

module.exports = EmailsEditor;